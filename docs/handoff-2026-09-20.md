# TravelStan handoff — 2026-09-20

This document captures the current state so work can continue in another
session without repeating the provider and runtime investigation.

## What the project is

TravelStan is a server-rendered Django 5.2 flight comparison app. It accepts
origin, destination, dates, cabin, date flexibility, and a checked-bag
requirement. Search results are normalized into offer cards with itinerary,
baggage, price, source status, freshness, and an optional safe booking link.

Search does not write to the database, cache, session, or logs. The app does
not sell tickets or take payment. It is intentionally JavaScript-free.

## Current code state

- Branch: `feature/live-providers-light-ui` (the name is stale; the UI is now dark).
- The branch started its current provider work from committed handoff SHA
  `354d713`.
- The active change set adds the constrained SerpApi experiment and removes
  AF–KLM, Singapore, and TUI from runtime selection while retaining their
  prototype source for reference.
- The full suite passes: 91 tests, Ruff, formatting, Django system checks, and
  `git diff --check`.

The current interface is a compact dark layout in
[`flights/static/flights/css/app.css`](../flights/static/flights/css/app.css).
The template uses a versioned stylesheet URL (`?v=20260920-dark`) so a browser
does not retain the old light CSS.

## Why the page showed the wrong UI

There are two different checkouts running on localhost:

| URL | Owner | Filesystem | State |
| --- | --- | --- | --- |
| `http://127.0.0.1:8001/` | GitHub Copilot supervisor | `~/.copilot/session-state/.../files/travelstan-released` | Released synthetic snapshot |
| `http://127.0.0.1:8002/` | This workspace | `/Users/stanvasilyev/projects/pet/travelstan` | Current dark build |

The Chrome tab was on port 8001. Its HTML literally contained the old title
“TravelStan demo flight comparison” and old `#080d18` metadata, while the
workspace files contained the new template. Port 8001 is controlled by another
process, so do not kill or repurpose it casually. Use port 8002 for this
workspace, or stop the Copilot task from the app that launched it.

To run this checkout:

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python manage.py runserver 127.0.0.1:8002 --noreload
```

Open `http://127.0.0.1:8002/` and hard-refresh if Chrome has already cached a
previous asset.

## Provider configuration and the current blocker

The running process is configured as:

```text
TRAVELSTAN_PROVIDERS=synthetic_demo
SERPAPI_API_KEY=missing
```

That is why the page shows generated itineraries. This is deliberate fail-closed
behavior: the app never presents fictional rows as real fares and never falls
back to synthetic results after an external provider fails.

The other session initially added these prototype adapters:

| Provider | Code status | What is still needed |
| --- | --- | --- |
| Air France–KLM Offers | Offline prototype; not runtime-selectable | Current version/schema, entitlement, quota, and terms validation |
| Singapore Airlines Flight Search | Offline prototype; not runtime-selectable | Correct official price/referral parsing and remove undocumented flexible parameters |
| TUI Flight Offers | Offline prototype; not runtime-selectable | Correct price/cabin/baggage mapping and implement the required session redirect flow |

No key is present in the workspace, and no provider account was created during
implementation. A real offer cannot be produced until at least one provider
key is supplied in the server process. Never put a populated key in this
repository or send it in chat.

Subsequent Astra research found that direct-airline APIs cannot provide the
required comparison variety and confirmed material mismatches in the
Singapore and TUI adapters. They are no longer runtime-selectable. The user
authorized a constrained SerpApi experiment as the sole external mode; see
`README.md`, `docs/operations.md`, and the top of
`docs/provider-research.md`.

Example experimental activation after obtaining a SerpApi key:

```bash
export TRAVELSTAN_PROVIDERS=serpapi
export SERPAPI_API_KEY='your-local-key'
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python manage.py check
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python manage.py runserver 127.0.0.1:8002 --noreload
```

The experiment intentionally renders no booking link and makes no checked-bag
claim.

## Provider investigation result

The original requirement was a lawful, custom comparison table with current
fares, bounded searches, honest baggage semantics, and a safe booking path.
The current findings are:

| Candidate | Free/self-service finding | Booking/data fit | Decision |
| --- | --- | --- | --- |
| Air France–KLM Offers | The current developer portal catalog marks Offers access as “Everyone” and says API usage is free of charge. An application and API key are still required. | Strongest fit among investigated options: ticket availability/prices and a path to complete booking on the airline website. Exact current quota and production approval still need validation with a real account. | Best next provider to activate |
| Singapore Airlines Flight Search | Official registration/trial access is available for the test environment. Production access requires contractual terms/approval and a production secret. | Useful native flight search and flexible-date request, but test data is not real inventory. | Keep as trial adapter; not a confirmed free production source |
| TUI Flight Offers | API key required; no confirmed recurring free allowance was found. | Flight offers and extras are available. Current live docs require initialize-session and update-session/in-start redirect calls before a reliable booking deep link. | Conditional; validate account and flow first |
| Turkish Airlines | Official portal documents a sandbox quota of 3,000/day, but production requires consultation/agreement. | Sandbox is not real bookable inventory; not integrated. | Not a free live solution |
| Amadeus | Current official site says the self-service portal was decommissioned on July 17; enterprise access remains. | Previously attractive API shape, but no new personal self-service path. | Remove from shortlist |
| Lufthansa Group | Registration was on hold in the earlier first-party check. | Partner access may provide airline links, but it is not currently obtainable as a personal self-service source. | Blocked |
| SerpApi Google Flights | Free plan advertises 250 successful searches/month, enough for exact searches at this scale. | It is a scraping intermediary; checked-bag proof, airline-direct links, retention, and amplification remain unsuitable under the original rules. | Only a separately approved personal experiment |
| Skyscanner | Partnership application rather than open self-service free access. | Booking-generation and partner restrictions conflict with the product boundary. | No-go |
| Travelpayouts | Search API rules require user-initiated full-result display and booking conversion; links may lead to agencies or brands. | Conflicts with comparison-only behavior and airline-direct proof. | No-go |
| Duffel | Commercial/booking-oriented offer-to-order product with look-to-book constraints. | Not suitable for this comparison-only app. | No-go |
| Google Flights partner access | Invite/NDA partner access. | Not a public developer path. | No-go |

Important source links:

- [Air France–KLM Offers overview](https://developer.airfranceklm.com/products/api/offers/docs/overview)
- [Air France–KLM current API catalog content](https://developer.airfranceklm.com/api/devportal/public/apis/offers/pages/overview/content)
- [Air France–KLM older key setup guide](https://klmprod.mashery.com/docs/read/opendata/Get_Started)
- [Singapore Airlines Flight Search](https://developer.singaporeair.com/docs/flight_search/flightavailability)
- [TUI Flight Offers API](https://developer.tui/api-catalog/flight-offers/api-description)
- [Turkish Airlines API onboarding](https://developer.apim.turkishairlines.com/howto)
- [Amadeus current developer homepage](https://developers.amadeus.com/)
- [SerpApi Google Flights](https://serpapi.com/google-flights-api) and [pricing](https://serpapi.com/pricing)

The canonical historical research and evidence matrix remains in
[`docs/provider-research.md`](provider-research.md). This handoff records the
new 2026-09-20 reassessment, including findings that still require credentialed
validation.

## Recommended continuation

1. Use port 8002 for the current workspace, or stop the Copilot-owned 8001
   task through Copilot rather than killing its child process repeatedly.
2. Obtain a personal SerpApi key outside the repository and export it only in
   the server process.
3. Validate one exact one-way and one exact round-trip search.
4. Confirm the SerpApi dashboard call count, returned dates, itinerary, price,
   currency, and absence of exposed credentials.
5. Keep exact mode as the default. Use `±1` only while the six-request ceiling
   and account quota leave sufficient headroom.
6. Never restore synthetic results after a failed SerpApi request.
